import { Point } from 'esri/geometry';
import { getLandsatScenes } from './getLandsatScenes';

export const getProfile = async (point: Point) => {
    // const landsatScenes = await getLandsatScenes({
    // })
};
