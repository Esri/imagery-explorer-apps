import React from 'react';

const Layout = () => {
    return (
        <div className="absolute bottom-0 left-0 bg-white h-32 w-60 z-10">
            Landsat Layout
        </div>
    );
};

export default Layout;
